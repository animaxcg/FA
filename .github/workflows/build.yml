name: runTests


on:
  - push
  - pull_request
jobs:
  docker:
    environment: build
    runs-on: ubuntu-latest
    steps:
      -
        name: RunTests
        run: |
          echo "Hello World"
          echo "test"
          sudo gem install bashcov
          pwd
          ls -l ${{ github.workspace }}
          ls -l ../
          ls -l tests
          bashcov tests/test_script.sh